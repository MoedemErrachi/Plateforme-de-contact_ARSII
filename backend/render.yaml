services:
  - type: web
    name: arsii-backend
    runtime: node
    region: frankfurt # eu — closest to Supabase eu-west-2
    plan: free
    rootDir: backend
    buildCommand: npm install --include=dev && npx prisma generate && npm run build && npx prisma migrate deploy
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      # ── Database (Supabase) ──
      - key: DATABASE_URL
        sync: false # set manually — contains credentials
      - key: DIRECT_URL
        sync: false
      # ── Supabase Storage ──
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      # ── Security ──
      - key: JWT_SECRET
        generateValue: true
      - key: CSRF_SECRET
        generateValue: true
      # ── Frontend URL (password-reset links + CORS) ──
      # sync: false — set in the Render dashboard (e.g. https://<frontend>.vercel.app).
      # Required in production: the backend refuses to start without them.
      - key: FRONTEND_URL
        sync: false
      - key: CORS_ORIGINS
        sync: false
      # ── SMTP ──
      - key: GMAIL_USER
        sync: false
      - key: GMAIL_APP_PASSWORD
        sync: false
