# ARSII Frontend (Vite)
# Vite dev server port
PORT=3000

# Backend URL (proxy Vite en dev ; redirigé par vercel.json en production)
VITE_BACKEND_URL="http://localhost:5000"

# Chatbot API URL (proxy Vite en dev ; redirigé par vercel.json en production)
VITE_CHATBOT_API_URL="http://localhost:8000"
